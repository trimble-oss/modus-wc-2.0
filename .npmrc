# Root install uses the public npm registry only.
# GitHub Packages auth for the private Blazor Razor output target lives in
# integrations/blazor/razor-output/.npmrc (see npm run install:blazor-tools).
