# Build artifacts — kept in sync by `npm run generate:blazor-stories`.
# Never commit these – they are regenerated automatically.
wwwroot/dist/
wwwroot/css/modus-wc-styles.css
wwwroot/iframe.html
Stories/

# .NET build output
obj/
bin/
