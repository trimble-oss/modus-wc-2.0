import { Meta } from '@storybook/blocks';

<Meta title="Documentation/Fallback gallery" />

# Theme fallback gallery

Open the [fallback gallery](/theme-generator/fallback-gallery.html) (live
Stencil tags plus CSS-class twins for Chrome &lt;111). Switch themes with the
dropdown. Token routing: `docs/custom-themes.md`.
