import { Meta } from '@storybook/blocks';

<Meta title="Documentation/Theme generator" />

# Offline theme generator

Edit `modus-theme.config.json` in your app and run `npx modus-theme build`.
Do not copy CSS out of this playground.

Open the [live JSON preview](/theme-generator/index.html) (import/export JSON
only). For a Rebuild button plus real components, run
`npm run theme:playground` and open
`/src/theme-cli/web/playground.html` on port 8771. Token contract:
`docs/custom-themes.md`.
