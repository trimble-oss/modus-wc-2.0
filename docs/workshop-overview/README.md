# Workshop overview

A 10-slide participant-facing deck. The detailed facilitator materials stay in `docs/workshop-slides/`.

## Open

- Google Slides: upload `workshop-overview.pptx`, then open with Google Slides.
- HTML: open `index.html` in a browser.

## Edit

1. Change `slides.mjs`.
2. Run `npm install` and `npm run build`.
